[project]
name = "sqe-fitting2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "András Márton Gunyhó", email = "andras.gunyho@riken.jp" }
]
requires-python = ">=3.13"
dependencies = [
    "xarray>=2026.7.0",
]

[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-xdist>=3.8.0",
]
